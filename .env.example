# Nanobot environment variables
# Copy to .env and fill in the values you need.
# All variables are optional — you can also configure via ~/.nanobot/config.json.

# ── LLM Providers ──────────────────────────────────────────────
#OPENROUTER_API_KEY=
#ANTHROPIC_API_KEY=
#OPENAI_API_KEY=
#DEEPSEEK_API_KEY=
#GROQ_API_KEY=
#GEMINI_API_KEY=
#DASHSCOPE_API_KEY=
#MOONSHOT_API_KEY=
#MINIMAX_API_KEY=
#ZAI_API_KEY=
#HOSTED_VLLM_API_KEY=

# ── Web Search ─────────────────────────────────────────────────
#BRAVE_API_KEY=

# ── MCP Servers (Model Context Protocol) ──────────────────────
# Configure MCP servers via config.json (recommended) or env vars.
# Env var format: NANOBOT_MCP__SERVERS__{NAME}__{FIELD}
# Example HTTP server:
# NANOBOT_MCP__SERVERS__GOOGLE_WORKSPACE__TYPE=http
# NANOBOT_MCP__SERVERS__GOOGLE_WORKSPACE__URL=http://localhost:8000/mcp
# Example stdio server:
# NANOBOT_MCP__SERVERS__MY_SERVER__TYPE=stdio
# NANOBOT_MCP__SERVERS__MY_SERVER__COMMAND=python
# See config.json "mcp.servers" section for full options.

# ── Nanobot Config Overrides (prefix NANOBOT_, nest with __) ───
# NANOBOT_GATEWAY__HOST=0.0.0.0
# NANOBOT_GATEWAY__PORT=18790

# ── Container User (match host user for file ownership) ───────
# NANOBOT_UID=1000
# NANOBOT_GID=1000

# ── Volume Override ────────────────────────────────────────────
# NANOBOT_CONFIG_DIR=~/.nanobot

# ── vLLM (only with --profile local-llm) ──────────────────────
# HUGGING_FACE_HUB_TOKEN=
# VLLM_MODEL=meta-llama/Llama-3.1-8B-Instruct
